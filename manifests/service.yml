apiVersion: v1
kind: Service
metadata:
    name: cassieluoliazurevotingappredis
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: cassieluoliazurevotingappredis